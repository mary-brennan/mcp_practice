[project]
name = "shellserver"
version = "0.1.0"
description = "A simple MCP server exposing a terminal tool"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "mcp[cli]>=2.2.0",
]
